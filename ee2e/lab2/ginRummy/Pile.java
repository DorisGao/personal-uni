package ginRummy;
import java.util.LinkedList;

/**
 * Pile is a class for representing a pile of {@link Card} objects.
 * Pile currently simply extends a LinkedList, but could have its own methods added in the future
 * 
 * @author Ben Francis and Sam Black
 */
class Pile extends LinkedList
{
	/*
	 * A pile is currently just inherits from LinkedList when instantiated
	 */
	public Pile()
	{
		super();
	}
}